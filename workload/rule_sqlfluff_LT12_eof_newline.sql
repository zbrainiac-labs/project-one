-- =============================================================================
-- SQLFluff LT12: EOF newline (file must end with single newline)
-- =============================================================================

-- POSITIVE TEST: This file ends with exactly one newline
SELECT SENSOR_ID FROM IOTI_RAW_TB_SENSOR_DATA;
