-- =============================================================================
-- SQLFluff LT14: Inconsistent line endings (must use LF, not CRLF)
-- =============================================================================

-- POSITIVE TEST: This file uses consistent LF line endings
SELECT SENSOR_ID FROM IOTI_RAW_TB_SENSOR_DATA;
SELECT SENSOR_0 FROM IOTI_RAW_TB_SENSOR_DATA;
